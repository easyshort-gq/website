<%- include('head', { title: 'easyshort.gq' }) -%>

<center>
    <form action="/getc" method="post" autocomplete="off">
        <h1>Check your <br class="rss"> link clicks!</h1>
        <input type="text" name="url" id="url" placeholder="Insert your short link here..." required>
        <button type="submit">Get stats</button>
        <% if(success) { %>
            <p>Your URL has <%= success %>!</p>
        <% } %>
        <% if(errors) { %>
            <p style="color: red;">The provided URL does not exist.</p>
        <% } %>
    </form>
</center>

<%- include("footer") -%>
</body>